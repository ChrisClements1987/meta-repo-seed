# Testing dependencies for Meta-Repo Seed
# Pinned versions for reproducible builds and security compliance

# Core testing framework
pytest==8.4.2
pytest-cov==7.0.0
pytest-mock==3.15.1

# Configuration and validation
PyYAML==6.0.2
jsonschema==4.25.1

# Security scanning (for license and vulnerability checking)
pip-licenses==5.0.0